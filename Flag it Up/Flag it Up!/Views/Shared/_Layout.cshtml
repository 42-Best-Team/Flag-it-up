<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Flag It Up</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">FlagItUp</a>

            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link text-warning fw-bold" asp-controller="Game" asp-action="Start">🎮 Грати</a>
                </li>

                @if (User.Identity?.IsAuthenticated ?? false)
                {
                    @if (User.IsInRole("Admin"))
                    {
                        <li class="nav-item"><a class="nav-link" href="/Admin">Адмінка</a></li>
                    }

                    <li class="nav-item"><a class="nav-link" href="/Account/Logout">Logout (@User.Identity.Name)</a></li>
                }
                else
                {
                    <li class="nav-item"><a class="nav-link" href="/Account/Login">Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Account/Register">Register</a></li>
                }
            </ul>
        </div>
    </nav>

    <div class="container mt-4">@RenderBody()</div>
</body>
</html>
