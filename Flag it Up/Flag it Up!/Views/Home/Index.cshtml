@model List<FlagItUpApp.Models.Flag>
@{
    Layout = "_Layout";
}
<h2>🌍 Прапори</h2>
<table class="table">
    <tr><th>Country</th><th>Region</th><th>Flag</th></tr>
    @foreach (var flag in Model)
    {
        <tr>
            <td>@flag.Country</td>
            <td>
                <img src="/@flag.ImagePath" alt="@flag.Country flag" class="img-fluid" style="max-width: 120px;" />

            </td>
        </tr>
    }
</table>